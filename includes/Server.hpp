#ifndef __SERVER_HPP_
#define __SERVER_HPP_

class Server
{
    public:
        Server();
        Server(const Server& rhs);
        Server& operator=(const Server& rhs);
        ~Server();
    private:
        /* data */ 
};
#endif